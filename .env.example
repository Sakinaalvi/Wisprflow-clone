# Copy this file to `.env` and fill in values to enable AI post-processing.

# OpenAI API key (only needed if you enable "AI Post-Processing" in settings)
OPENAI_API_KEY=

# AI model for post-processing (default: gpt-4o-mini)
AI_MODEL=gpt-4o-mini
